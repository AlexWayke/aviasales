import Flex from '@Components/flex/flex';
import './header.scss';

function Header() {
  return (
    <Flex classes="header">
      <img src="Logo.png" alt="Logo" />
    </Flex>
  );
}

export default Header;
